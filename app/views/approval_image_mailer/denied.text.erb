Your sended image for
<%= @approval_image.product.display %> 
was approved.

Denied image here:
<%= url_for @approval_image.image.url %>

You can try add a new one.
<%= url_for @approval_image.product %>

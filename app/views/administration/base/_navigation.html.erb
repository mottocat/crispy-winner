<%= link_to_unless_current "DASBOARD",
                            administration_path,
                            class: "link dib mr3 blue" %>

<%= link_to_unless_current "USERS (" + User.count.to_s + ")",
                            administration_users_path,
                            class: "link dib mr3 blue" %>

<%= link_to_unless_current "PRODUCTS (" + Product.count.to_s + ")",
                            administration_products_path,
                            class: "link dib mr3 blue" %>

<%= link_to_unless_current "COMMENTS (" + Comment.count.to_s + ")",
                            administration_comments_path,
                            class: "link dib mr3 blue" %>

<%= link_to_unless_current "USAGE APPROVAL (" + ApprovalImage.count.to_s + ")",
                            administration_usage_approvals_path,
                            class: "link dib mr3 blue" %>

<hr class="b--black-20 mt3" />